// https://codeforces.com/problemset/problem/1804/B

#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--){
        int n, k exp, wait;
        vector<int> arrival;
        cin >> n >> k >> exp >> wait;
        for(int i = 0; i < n; i++){
            int num;
            cin >> num;
            arrival.push_back(num);
        }
        
    }
}