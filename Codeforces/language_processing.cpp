#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    // Vowels = a, e
    // Consonants = b,c,d

    while(t--) {
        int n;
        cin >> n;

        string word;
        cin >> word;

        int length = word.size();

        for(int i = length; i >= 0; i++) {
            
        }
    }
}